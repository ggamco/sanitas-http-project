import { SanitasMultiplicarDirective } from './sanitas-multiplicar.directive';

describe('SanitasMultiplicarDirective', () => {
  it('should create an instance', () => {
    const directive = new SanitasMultiplicarDirective();
    expect(directive).toBeTruthy();
  });
});
