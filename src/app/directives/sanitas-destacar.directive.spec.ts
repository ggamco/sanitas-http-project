import { SanitasDestacarDirective } from './sanitas-destacar.directive';

describe('SanitasDestacarDirective', () => {
  it('should create an instance', () => {
    const directive = new SanitasDestacarDirective();
    expect(directive).toBeTruthy();
  });
});
